{
  "name": "first-fm",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && set PORT=5000 && npm start",
    "client": "cd frontend && set PORT=3000 && npm start"
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
